<UserControl x:Class="CSM.App.Workspaces.CustomLevels.CustomLevelListView"
             xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
             xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
             xmlns:mc="http://schemas.openxmlformats.org/markup-compatibility/2006" 
             xmlns:d="http://schemas.microsoft.com/expression/blend/2008" 
             xmlns:local="clr-namespace:CSM.App.Workspaces.CustomLevels" xmlns:telerik="http://schemas.telerik.com/2008/xaml/presentation"
             mc:Ignorable="d" 
             d:DesignHeight="450" d:DesignWidth="800">
    <UserControl.Resources>
        <telerik:StringToGlyphConverter x:Key="StringToGlyphConverter" />
    </UserControl.Resources>
    
    <Grid>
        <Grid.RowDefinitions>
            <RowDefinition Height="32" />
            <RowDefinition Height="*" />
        </Grid.RowDefinitions>
        <!-- Header -->
        <Border Background="#FF018574" >
            <TextBlock Text="Custom Levels" Foreground="White" VerticalAlignment="Center" Margin="8,0" />
        </Border>
        <!-- Content -->
        <Border Grid.Row="1" BorderBrush="DimGray" BorderThickness="1,0,1,1">
            <telerik:RadGridView ItemsSource="{Binding CustomLevels}" SelectedItem="{Binding SelectedCustomLevel}" AutoGenerateColumns="False" RowIndicatorVisibility="Collapsed" FrozenColumnsSplitterVisibility="Collapsed">
                <telerik:RadGridView.Columns>
                    <telerik:GridViewDataColumn Header="BSR" DataMemberBinding="{Binding BsrKey}" />
                    <telerik:GridViewDataColumn Header="Song Name" DataMemberBinding="{Binding SongName}" />
                    <telerik:GridViewDataColumn Header="Mapper" DataMemberBinding="{Binding LevelAuthorName}" />
                    <telerik:GridViewDataColumn Header="Composer" DataMemberBinding="{Binding SongAuthorName}" />
                    <telerik:GridViewDataColumn Header="E" DataMemberBinding="{Binding Easy}">
                        <telerik:GridViewDataColumn.CellEditTemplate>
                            <DataTemplate>
                                <TextBlock Text="&#xe118;" Foreground="#FF018574" />
                            </DataTemplate>
                        </telerik:GridViewDataColumn.CellEditTemplate>
                    </telerik:GridViewDataColumn>
                    <telerik:GridViewDataColumn Header="N" DataMemberBinding="{Binding Normal}" />
                    <telerik:GridViewDataColumn Header="H" DataMemberBinding="{Binding Hard}" />
                    <telerik:GridViewDataColumn Header="Ex" DataMemberBinding="{Binding Expert}" />
                    <telerik:GridViewDataColumn Header="Ex+" DataMemberBinding="{Binding ExpertPlus}" />
                    <telerik:GridViewDataColumn Header="Change Date" DataMemberBinding="{Binding ChangeDate}" />
                </telerik:RadGridView.Columns>
            </telerik:RadGridView>
        </Border>
    </Grid>
</UserControl>
