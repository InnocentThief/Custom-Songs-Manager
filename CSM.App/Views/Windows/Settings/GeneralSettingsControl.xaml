<UserControl x:Class="CSM.App.Views.Windows.Settings.GeneralSettingsControl"
             xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
             xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
             xmlns:mc="http://schemas.openxmlformats.org/markup-compatibility/2006" 
             xmlns:d="http://schemas.microsoft.com/expression/blend/2008" 
             xmlns:local="clr-namespace:CSM.App.Views.Windows.Settings"
             xmlns:telerik="http://schemas.telerik.com/2008/xaml/presentation" 
             mc:Ignorable="d" 
             d:DesignHeight="450" d:DesignWidth="800">
    <Grid Margin="0,6">
        <Grid.ColumnDefinitions>
            <ColumnDefinition Width="*" />
            <ColumnDefinition Width="*"/>
        </Grid.ColumnDefinitions>
        <Grid.RowDefinitions>
            <RowDefinition Height="Auto" />
            <RowDefinition Height="Auto" />
            <RowDefinition Height="Auto" />
        </Grid.RowDefinitions>
        <!-- Default Workspace -->
        <telerik:Label Content="Default workspace" />
        <telerik:RadComboBox Grid.Column="1" Focusable="False" ItemsSource="{Binding Workspaces}" DisplayMemberPath="DisplayText" SelectedItem="{Binding SelectedWorkspace}" />
        <!-- Beat Saber Install Path -->
        <telerik:Label Content="Beat Saber installation path" Grid.Row="1" />
        <telerik:RadFilePathPicker Grid.Column="1" Grid.Row="1" DialogType="OpenFolder" FilePath="{Binding BeatSaberInstallationPath}" Text="{Binding BeatSaberInstallationPath}" FilePathChanged="RadFilePathPicker_FilePathChanged" />
        <!-- BeatSaver API endpoint -->
        <telerik:Label Content="BeatSaver API endpoint" Grid.Row="2" />
        <telerik:RadWatermarkTextBox Text="{Binding BeatSaverAPIEndpoint}" Grid.Column="1" Grid.Row="2" />
    </Grid>
</UserControl>
