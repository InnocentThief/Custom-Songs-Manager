<UserControl x:Class="CSM.App.Views.Windows.Settings.CustomLevelsSettingsControl"
             xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
             xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
             xmlns:mc="http://schemas.openxmlformats.org/markup-compatibility/2006" 
             xmlns:d="http://schemas.microsoft.com/expression/blend/2008" 
             xmlns:local="clr-namespace:CSM.App.Views.Windows.Settings"
             xmlns:telerik="http://schemas.telerik.com/2008/xaml/presentation" 
             mc:Ignorable="d" 
             d:DesignHeight="450" d:DesignWidth="800">
    <Grid Margin="0,6">
        <Grid.ColumnDefinitions>
            <ColumnDefinition Width="*" />
            <ColumnDefinition Width="*" />
        </Grid.ColumnDefinitions>
        <Grid.RowDefinitions>
            <RowDefinition Height="Auto" />
            <RowDefinition Height="Auto" />
            <RowDefinition Height="Auto" />
        </Grid.RowDefinitions>
        <!-- Availability -->
        <telerik:Label Content="Available" />
        <telerik:RadToggleSwitchButton Grid.Column="1"  Focusable="False" IsChecked="{Binding Available}" />
        <!-- Custom Levels Path -->
        <telerik:Label Content="Custom levels path" VerticalAlignment="Center" Grid.Row="1" />
        <telerik:RadFilePathPicker Grid.Column="1" Grid.Row="1" DialogType="OpenFolder" FilePath="{Binding CustomLevelsPath}" Text="{Binding CustomLevelsPath}" FilePathChanged="RadFilePathPicker_FilePathChanged" />
        <!-- Song Detail Position -->
        <telerik:Label Content="Song detail position" Grid.Row="2" />
        <telerik:RadComboBox Grid.Column="1" Grid.Row="2" Focusable="False" ItemsSource="{Binding SongDetailPositions}" SelectedItem="{Binding SelectedSongDetailPosition}" DisplayMemberPath="DisplayText" />
    </Grid>
</UserControl>
