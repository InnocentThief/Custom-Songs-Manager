<UserControl x:Class="CSM.App.Views.Windows.Settings.SongSuggestSettingsControl"
             xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
             xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
             xmlns:mc="http://schemas.openxmlformats.org/markup-compatibility/2006" 
             xmlns:d="http://schemas.microsoft.com/expression/blend/2008" 
             xmlns:local="clr-namespace:CSM.App.Views.Windows.Settings"
             xmlns:telerik="http://schemas.telerik.com/2008/xaml/presentation"
             mc:Ignorable="d" 
             d:DesignHeight="450" d:DesignWidth="800">
    <Grid Margin="0,6">
        <Grid.ColumnDefinitions>
            <ColumnDefinition Width="*" />
            <ColumnDefinition Width="*" />
        </Grid.ColumnDefinitions>
        <Grid.RowDefinitions>
            <RowDefinition Height="Auto" />
            <RowDefinition Height="Auto" />
            <RowDefinition Height="Auto" />
            <RowDefinition Height="Auto" />
            <RowDefinition Height="Auto" />
        </Grid.RowDefinitions>
        <!-- ScoreSaber User Id -->
        <telerik:Label Content="ScoreSaber User Id" />
        <telerik:RadWatermarkTextBox Grid.Column="1" Text="{Binding ScoreSaberUserId, UpdateSourceTrigger=PropertyChanged}" />
        <!-- BeatLeader User Id -->
        <telerik:Label Content="BeatLeader User Id" Grid.Row="1" />
        <telerik:RadWatermarkTextBox Grid.Column="1" Grid.Row="1" Text="{Binding BeatLeaderUserId, UpdateSourceTrigger=PropertyChanged}" />
        <!-- UseScoreSaberLeaderboard -->
        <telerik:Label Content="Use ScoreSaber Leaderboard" Grid.Row="2" />
        <telerik:RadToggleSwitchButton Grid.Column="1" Grid.Row="2" Focusable="False" IsChecked="{Binding UseScoreSaberLeaderboard}" />
        <!-- UseBeatLeaderLeaderboard -->
        <telerik:Label Content="Use BeatLeader Leaderboard" Grid.Row="3" />
        <telerik:RadToggleSwitchButton Grid.Column="1" Grid.Row="3" Focusable="False" IsChecked="{Binding UseBeatLeaderLeaderboard}" />
        <!-- Default Leaderboard -->
        <telerik:Label Content="Default Leaderboard" Grid.Row="4" />
        <telerik:RadComboBox Grid.Column="1" Grid.Row="4" Focusable="False" ItemsSource="{Binding Leaderboards}" SelectedItem="{Binding SelectedLeaderboard}" DisplayMemberPath="DisplayText" />
    </Grid>
</UserControl>
