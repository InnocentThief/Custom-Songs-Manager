<UserControl x:Class="CSM.App.Views.Workspaces.PlaylistsWorkspace"
             xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
             xmlns:d="http://schemas.microsoft.com/expression/blend/2008"
             xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
             xmlns:mc="http://schemas.openxmlformats.org/markup-compatibility/2006"
             xmlns:telerik="http://schemas.telerik.com/2008/xaml/presentation"
             xmlns:playlists="clr-namespace:CSM.App.Views.Controls.Playlists"
             xmlns:songsources="clr-namespace:CSM.App.Views.Controls.SongSources"
             mc:Ignorable="d" 
             d:DesignHeight="450" d:DesignWidth="800">

    <Grid>
        <Grid.RowDefinitions>
            <RowDefinition Height="27" />
            <RowDefinition Height="*" />
        </Grid.RowDefinitions>
        <telerik:RadRibbonView ApplicationButtonVisibility="Collapsed" LayoutMode="Simplified" ApplicationName="{Binding Title}" />
        <Border Grid.Row="1" Style="{StaticResource WorkspaceAreaStyle}">
            <Grid>
                <Grid.ColumnDefinitions>
                    <ColumnDefinition Width="1*" MinWidth="50" />
                    <ColumnDefinition Width="3" />
                    <ColumnDefinition Width="2*" MinWidth="50" />
                    <ColumnDefinition Width="3" />
                    <ColumnDefinition Width="3*" MinWidth="90" />
                </Grid.ColumnDefinitions>
                <playlists:PlaylistsTreeControl DataContext="{Binding PlaylistsTree}" />
                <GridSplitter Grid.Column="1" Style="{StaticResource WorkspaceVerticalGridSplitterStyle}" HorizontalAlignment="Center" />
                <playlists:PlaylistControl Grid.Column="2" DataContext="{Binding PlaylistsTree.SelectedPlaylist}" />
                <GridSplitter Grid.Column="3" Style="{StaticResource WorkspaceVerticalGridSplitterStyle}" HorizontalAlignment="Center" />
                <songsources:SongSourcesControl Grid.Column="4" DataContext="{Binding SongSources}" />
            </Grid>
        </Border>
    </Grid>

</UserControl>
