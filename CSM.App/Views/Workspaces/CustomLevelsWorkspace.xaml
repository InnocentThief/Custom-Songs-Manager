<UserControl x:Class="CSM.App.Views.Workspaces.CustomLevelsWorkspace"
             xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
             xmlns:d="http://schemas.microsoft.com/expression/blend/2008"
             xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
             xmlns:mc="http://schemas.openxmlformats.org/markup-compatibility/2006"
             xmlns:telerik="http://schemas.telerik.com/2008/xaml/presentation"
             xmlns:common="clr-namespace:CSM.App.Views.Common"
             xmlns:customlevels="clr-namespace:CSM.App.Views.Controls.CustomLevels"
             xmlns:customlevels1="clr-namespace:CSM.App.Views.Workspaces.CustomLevels"
             mc:Ignorable="d" 
             d:DesignHeight="450" d:DesignWidth="800">

    <UserControl.Resources>
        <DataTemplate x:Key="SongDetailRightDataTemplate">
            <Grid>
                <Grid.ColumnDefinitions>
                    <ColumnDefinition Width="*" MinWidth="100" />
                    <ColumnDefinition Width="3" />
                    <ColumnDefinition Width="220" MinWidth="50" />
                </Grid.ColumnDefinitions>
                <customlevels:CustomLevelsControl />
                <GridSplitter Grid.Column="1" Style="{StaticResource WorkspaceVerticalGridSplitterStyle}" HorizontalAlignment="Center" />

                <common:MapDetailNoDataControl Grid.Column="2" Visibility="{Binding HasSelectedCustomLevel, Converter={StaticResource InvertedBooleanToVisibilityConverter}}" />

                <Grid Grid.Column="2" Visibility="{Binding HasSelectedCustomLevel, Converter={StaticResource BooleanToVisibilityConverter}}">
                    <common:MapDetailVerticalControl DataContext="{Binding SelectedCustomLevel.MapDetailViewModel}" />
                </Grid>
            </Grid>
        </DataTemplate>

        <DataTemplate x:Key="SongDetailBottomDataTemplate">
            <Grid>
                <Grid.RowDefinitions>
                    <RowDefinition Height="*" MinHeight="100" />
                    <RowDefinition Height="3" />
                    <RowDefinition Height="220" MinHeight="50" />
                </Grid.RowDefinitions>
                <customlevels:CustomLevelsControl />
                <GridSplitter Grid.Row="1" Style="{StaticResource WorkspaceHorizontalGridSplitterStyle}" VerticalAlignment="Center" />

                <common:MapDetailNoDataControl Grid.Row="2" Visibility="{Binding HasSelectedCustomLevel, Converter={StaticResource InvertedBooleanToVisibilityConverter}}" />

                <Grid Grid.Row="2" Visibility="{Binding HasSelectedCustomLevel, Converter={StaticResource BooleanToVisibilityConverter}}">
                    <common:MapDetailHorizontalControl DataContext="{Binding SelectedCustomLevel.MapDetailViewModel}" />
                </Grid>
            </Grid>
        </DataTemplate>

        <customlevels1:SongDetailPositionContentTemplateSelector x:Key="SongDetailPositionContentTemplateSelector"
                                                                 SongDetailRightDataTemplate="{StaticResource SongDetailRightDataTemplate}"
                                                                 SongDetailBottomDataTemplate="{StaticResource SongDetailBottomDataTemplate}"/>
    </UserControl.Resources>

    <Grid>
        <Grid.RowDefinitions>
            <RowDefinition Height="27" />
            <RowDefinition Height="*" />
        </Grid.RowDefinitions>
        <telerik:RadRibbonView ApplicationButtonVisibility="Collapsed" LayoutMode="Simplified" ApplicationName="{Binding Title}" />
        <Border Grid.Row="1" Style="{StaticResource WorkspaceAreaStyle}">
            <ContentControl Content="{Binding CustomLevelsControl}" ContentTemplateSelector="{StaticResource SongDetailPositionContentTemplateSelector}" />
        </Border>
    </Grid>

</UserControl>
