<UserControl x:Class="CSM.App.Views.Workspaces.CustomLevelsWorkspace"
             xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
             xmlns:d="http://schemas.microsoft.com/expression/blend/2008"
             xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
             xmlns:mc="http://schemas.openxmlformats.org/markup-compatibility/2006"
             xmlns:telerik="http://schemas.telerik.com/2008/xaml/presentation"
             xmlns:customlevels="clr-namespace:CSM.App.Views.Controls.CustomLevels" xmlns:common="clr-namespace:CSM.App.Views.Common"
             mc:Ignorable="d" 
             d:DesignHeight="450" d:DesignWidth="800">

    <Grid>
        <Grid.RowDefinitions>
            <RowDefinition Height="27" />
            <RowDefinition Height="*" />
        </Grid.RowDefinitions>
        <telerik:RadRibbonView ApplicationButtonVisibility="Collapsed" LayoutMode="Simplified" ApplicationName="{Binding Title}" />
        <Border Grid.Row="1" Style="{StaticResource WorkspaceAreaStyle}">
            <Grid>
                <!-- Horizontal Detail -->
                <Grid>
                    <Grid.RowDefinitions>
                        <RowDefinition Height="*" />
                        <RowDefinition Height="3" />
                        <RowDefinition Height="200" MinHeight="50" />
                    </Grid.RowDefinitions>
                    <customlevels:CustomLevelsControl DataContext="{Binding CustomLevelsControl}" />
                    <GridSplitter Grid.Row="1" Style="{StaticResource WorkspaceHorizontalGridSplitterStyle}" VerticalAlignment="Center" />

                    <common:SongDetailNoDataControl Grid.Row="2" Visibility="{Binding CustomLevelsControl.HasSelectedCustomLevel, Converter={StaticResource InvertedBooleanToVisibilityConverter}}" />

                    <Grid Grid.Row="2" Visibility="{Binding CustomLevelsControl.HasSelectedCustomLevel, Converter={StaticResource BooleanToVisibilityConverter}}">
                        <common:SongDetailHorizontalControl DataContext="{Binding CustomLevelsControl.SelectedCustomLevel}" />
                    </Grid>
                </Grid>

                <!-- Vertical Detail -->
                <Grid>
                    <Grid.ColumnDefinitions>
                        <ColumnDefinition Width="*" />
                        <ColumnDefinition Width="3" />
                        <ColumnDefinition Width="200" MinWidth="50" />
                    </Grid.ColumnDefinitions>
                    <customlevels:CustomLevelsControl DataContext="{Binding CustomLevelsControl}" />
                    <GridSplitter Grid.Column="1" Style="{StaticResource WorkspaceVerticalGridSplitterStyle}" HorizontalAlignment="Center" />

                    <common:SongDetailNoDataControl Grid.Column="2" Visibility="{Binding CustomLevelsControl.HasSelectedCustomLevel, Converter={StaticResource InvertedBooleanToVisibilityConverter}}" />

                    <Grid Grid.Column="2" Visibility="{Binding CustomLevelsControl.HasSelectedCustomLevel, Converter={StaticResource BooleanToVisibilityConverter}}">
                        <common:SongDetailVerticalControl DataContext="{Binding CustomLevelsControl.SelectedCustomLevel}" />
                    </Grid>
                </Grid>
            </Grid>
        </Border>
    </Grid>

</UserControl>
