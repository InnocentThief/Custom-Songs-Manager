<UserControl x:Class="CSM.App.Views.Workspaces.ScoreSaberWorkspace"
             xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
             xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
             xmlns:mc="http://schemas.openxmlformats.org/markup-compatibility/2006" 
             xmlns:d="http://schemas.microsoft.com/expression/blend/2008" 
             xmlns:telerik="http://schemas.telerik.com/2008/xaml/presentation"
             xmlns:local="clr-namespace:CSM.App.Views.Workspaces"
             mc:Ignorable="d" 
             d:DesignHeight="450" d:DesignWidth="800">

    <Grid>
        <Grid.RowDefinitions>
            <RowDefinition Height="27" />
            <RowDefinition Height="*" />
        </Grid.RowDefinitions>
        <telerik:RadRibbonView ApplicationButtonVisibility="Collapsed" LayoutMode="Simplified" ApplicationName="{Binding Title}" />
        <Border Grid.Row="1" Style="{StaticResource WorkspaceAreaStyle}">
            <Grid>
                <Grid.RowDefinitions>
                    <RowDefinition Height="*" />
                    <RowDefinition Height="3" />
                    <RowDefinition Height="200" MinHeight="50" />
                </Grid.RowDefinitions>
                <Grid>

                </Grid>
                <GridSplitter Grid.Row="1" Style="{StaticResource WorkspaceHorizontalGridSplitterStyle}" VerticalAlignment="Center" />
                <Grid Grid.Row="2">

                </Grid>
            </Grid>
        </Border>
    </Grid>

</UserControl>
