<UserControl x:Class="CSM.App.Views.Workspaces.BeatLeaderWorkspace"
             xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
             xmlns:d="http://schemas.microsoft.com/expression/blend/2008"
             xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
             xmlns:mc="http://schemas.openxmlformats.org/markup-compatibility/2006"
             xmlns:telerik="http://schemas.telerik.com/2008/xaml/presentation"
             xmlns:beatleader="clr-namespace:CSM.App.Views.Controls.BeatLeader"
             mc:Ignorable="d" 
             d:DesignHeight="450" d:DesignWidth="800">

    <Grid>
        <Grid.RowDefinitions>
            <RowDefinition Height="27" />
            <RowDefinition Height="*" />
        </Grid.RowDefinitions>
        <telerik:RadRibbonView ApplicationButtonVisibility="Collapsed" LayoutMode="Simplified" ApplicationName="{Binding Title}" />
        <Border Grid.Row="1" Style="{StaticResource WorkspaceAreaStyle}">
            <beatleader:BeatLeaderControl DataContext="{Binding BeatLeaderControl}" />
        </Border>
    </Grid>

</UserControl>
