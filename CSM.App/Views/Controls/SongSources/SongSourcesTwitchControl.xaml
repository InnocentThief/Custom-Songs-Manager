<UserControl x:Class="CSM.App.Views.Controls.SongSources.SongSourcesTwitchControl"
             xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
             xmlns:d="http://schemas.microsoft.com/expression/blend/2008"
             xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
             xmlns:mc="http://schemas.openxmlformats.org/markup-compatibility/2006"
             xmlns:telerik="http://schemas.telerik.com/2008/xaml/presentation"
             xmlns:twitch="clr-namespace:CSM.App.Views.Controls.SongSources.Twitch"
             mc:Ignorable="d" 
             d:DesignHeight="450" d:DesignWidth="800">
    <Grid>
        <Grid.RowDefinitions>
            <RowDefinition Height="Auto" />
            <RowDefinition Height="*" />
        </Grid.RowDefinitions>
        <!-- Header -->
        <Grid Background="#88018574">
            <Grid.ColumnDefinitions>
                <ColumnDefinition Width="*" />
                <ColumnDefinition Width="Auto" />
            </Grid.ColumnDefinitions>
            <!-- Title -->
            <TextBlock Text="Twitch" Style="{StaticResource HeaderTitleStyle}" />
            <!-- Commands -->
            <telerik:RadToolBar Grid.Column="1" Style="{StaticResource HeaderToolBarStyle}" >
                <TextBlock Text="{Binding Username}"/>
                <telerik:RadButton Command="{Binding LoginCommand}" ToolTip="Login to Twitch" Visibility="{Binding LoggedIn, Converter={StaticResource InvertedBooleanToVisibilityConverter}}">
                    <telerik:RadGlyph Glyph="&#xe130;" />
                </telerik:RadButton>
                <telerik:RadButton Command="{Binding LogoutCommand}" ToolTip="Logout from Twitch" Visibility="{Binding LoggedIn, Converter={StaticResource BooleanToVisibilityConverter}}">
                    <telerik:RadGlyph Glyph="&#xe131;" />
                </telerik:RadButton>
            </telerik:RadToolBar>
        </Grid>

        <twitch:NotLoggedInControl Grid.Row="1" Visibility="{Binding LoggedIn, Converter={StaticResource InvertedBooleanToVisibilityConverter}}"/>

        <Grid Grid.Row="1" Visibility="{Binding LoggedIn, Converter={StaticResource BooleanToVisibilityConverter}}">
            <twitch:LoggedInControl Visibility="{Binding Connected, Converter={StaticResource BooleanToVisibilityConverter}}" />
            <twitch:NotConnectedControl Visibility="{Binding Connected, Converter={StaticResource InvertedBooleanToVisibilityConverter}}" />
        </Grid>

    </Grid>
</UserControl>
