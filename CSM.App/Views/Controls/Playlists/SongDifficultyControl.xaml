<UserControl x:Class="CSM.App.Views.Controls.Playlists.SongDifficultyControl"
             xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
             xmlns:d="http://schemas.microsoft.com/expression/blend/2008"
             xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
             xmlns:mc="http://schemas.openxmlformats.org/markup-compatibility/2006"
             xmlns:telerik="http://schemas.telerik.com/2008/xaml/presentation"
             mc:Ignorable="d" 
             d:DesignHeight="450" d:DesignWidth="800">

    <!-- List with difficulties -->
    <StackPanel Orientation="Horizontal">
        <ItemsControl ItemsSource="{Binding Difficulties}" Background="Transparent" BorderThickness="0" Padding="0" Margin="0">
            <ItemsControl.ItemsPanel>
                <ItemsPanelTemplate>
                    <VirtualizingStackPanel IsItemsHost="True" Orientation="Horizontal" />
                </ItemsPanelTemplate>
            </ItemsControl.ItemsPanel>
            <ItemsControl.ItemTemplate>
                <DataTemplate>
                    <ContentControl Content="{Binding}" ContentTemplateSelector="{StaticResource CharacteristicDataTemplateSelector}" Margin="1" />
                </DataTemplate>
            </ItemsControl.ItemTemplate>
        </ItemsControl>

        <!-- To hide the button, to ensure the characteristic icon shape -->
        <Grid>
            <telerik:RadButton Background="Transparent" BorderThickness="0"  />
            <Border Background="Transparent" />
        </Grid>
    </StackPanel>
</UserControl>
