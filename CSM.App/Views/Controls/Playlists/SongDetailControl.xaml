<UserControl x:Class="CSM.App.Views.Controls.Playlists.SongDetailControl"
             xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
             xmlns:d="http://schemas.microsoft.com/expression/blend/2008"
             xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
             xmlns:mc="http://schemas.openxmlformats.org/markup-compatibility/2006"
             xmlns:telerik="http://schemas.telerik.com/2008/xaml/presentation"
             xmlns:local="clr-namespace:CSM.App.Views.Controls.Playlists"
             mc:Ignorable="d" 
             d:DesignHeight="450" d:DesignWidth="800">

    <UserControl.Resources>
        <DataTemplate x:Key="NoSongDataTemplate">
            <Grid VerticalAlignment="Center" HorizontalAlignment="Center">
                <Grid.RowDefinitions>
                    <RowDefinition Height="Auto" />
                    <RowDefinition Height="Auto" />
                </Grid.RowDefinitions>
                <telerik:RadGlyph Glyph="&#xe023;" FontSize="30" />
                <TextBlock Grid.Row="1" Text="No song selected" Margin="0,10" />
            </Grid>
        </DataTemplate>

        <DataTemplate x:Key="SongDataTemplate">
            
        </DataTemplate>

        <local:SongDetailDataTemplateSelector x:Key="SongDetailDataTemplateSelector"
                                                  NoSongDataTemplate="{StaticResource NoSongDataTemplate}"
                                                  SongDataTemplate="{StaticResource SongDataTemplate}"/>
    </UserControl.Resources>
    
    <Grid>
        <Grid.RowDefinitions>
            <RowDefinition Height="40" />
            <RowDefinition Height="*" />
        </Grid.RowDefinitions>
        <!-- Header -->
        <Grid>
            <Grid.ColumnDefinitions>
                <ColumnDefinition Width="*" />
                <ColumnDefinition Width="Auto" />
            </Grid.ColumnDefinitions>
            <!-- Title -->
            <TextBlock Text="Song Detail" Style="{StaticResource HeaderTitleStyle}" Foreground="#88018574" />
            <!-- Commands -->
            <telerik:RadToolBar Grid.Column="1" Style="{StaticResource HeaderToolBarStyle}" >
                <telerik:RadButton ToolTip="Preview song">
                    <telerik:RadGlyph Glyph="&#xe200;" Foreground="#88018574" />
                </telerik:RadButton>
                <telerik:RadButton ToolTip="Copy BSR key">
                    <telerik:RadGlyph Glyph="&#xe111;" Foreground="#88018574" />
                </telerik:RadButton>
            </telerik:RadToolBar>
        </Grid>
        <!-- Content -->
        <ContentControl Grid.Row="1" Content="{Binding SelectedSong}" />
    </Grid>
</UserControl>
