<UserControl x:Class="CSM.App.Views.Controls.Navigation.Navigation"
             xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
             xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
             xmlns:mc="http://schemas.openxmlformats.org/markup-compatibility/2006" 
             xmlns:d="http://schemas.microsoft.com/expression/blend/2008" 
             xmlns:telerik="http://schemas.telerik.com/2008/xaml/presentation"
             xmlns:local="clr-namespace:CSM.App.Views.Controls.Navigation"
             mc:Ignorable="d">

    <UserControl.Resources>
        <telerik:StringToGlyphConverter x:Key="StringToGlyphConverter" />

        <DataTemplate x:Key="NavigationItemTemplate">
            <telerik:RadRadioButton GroupName="MainNavigationGroup" Focusable="False" IsChecked="{Binding IsSelected}" Padding="8" ToolTip="{Binding DisplayName}">
                <telerik:RadGlyph Glyph="{Binding IconGlyph, Converter={StaticResource StringToGlyphConverter}}" />
            </telerik:RadRadioButton>
        </DataTemplate>
    </UserControl.Resources>
    
    <StackPanel VerticalAlignment="Center">
        <ItemsControl ItemsSource="{Binding Items}" ItemTemplate="{StaticResource NavigationItemTemplate }" />
        <Separator Margin="0,10" /> 
        <telerik:RadButton Focusable="False" Padding="8">
            <telerik:RadGlyph Glyph="&#xe401;" />
        </telerik:RadButton>
        <telerik:RadButton Focusable="False" Padding="8">
            <telerik:RadGlyph Glyph="&#xe13a;" />
        </telerik:RadButton>
    </StackPanel>

</UserControl>
