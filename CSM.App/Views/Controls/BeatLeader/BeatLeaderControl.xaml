<UserControl x:Class="CSM.App.Views.Controls.BeatLeader.BeatLeaderControl"
             xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
             xmlns:d="http://schemas.microsoft.com/expression/blend/2008"
             xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
             xmlns:mc="http://schemas.openxmlformats.org/markup-compatibility/2006"
             xmlns:telerik="http://schemas.telerik.com/2008/xaml/presentation"
             xmlns:common="clr-namespace:CSM.App.Views.Common"
             xmlns:local="clr-namespace:CSM.App.Views.Controls.BeatLeader"
             mc:Ignorable="d" 
             d:DesignHeight="450" d:DesignWidth="800">

    <telerik:RadBusyIndicator IsBusy="{Binding LoadingInProgress}" BusyContent="{Binding LoadingInProgressMessage}">
        <Grid>
            <Grid.RowDefinitions>
                <RowDefinition Height="40" />
                <RowDefinition Height="Auto" />
                <RowDefinition Height="*" />
            </Grid.RowDefinitions>

            <!-- Header -->
            <Grid Background="#88018574">
                <Grid.ColumnDefinitions>
                    <ColumnDefinition Width="*" />
                    <ColumnDefinition Width="Auto" />
                </Grid.ColumnDefinitions>
                <!-- Title -->
                <TextBlock Text="BeatLeader" Style="{StaticResource HeaderTitleStyle}" />
                <!-- Commands -->
                <telerik:RadToolBar Grid.Column="1" Style="{StaticResource HeaderToolBarNoGripStyle}" >
                    <telerik:RadButton Command="{Binding SwichPlayerCommand}" ToolTip="Switch player">
                        <telerik:RadGlyph Glyph="&#xe801;" />
                    </telerik:RadButton>
                </telerik:RadToolBar>
            </Grid>

            <!-- Player -->
            <local:PlayerControl Grid.Row="1" DataContext="{Binding Player}" />

            <!-- Scores -->
            <local:ScoreControl Grid.Row="2" />

            <!-- Player search -->
            <Grid Grid.RowSpan="3" Background="Black" Opacity="0.9" Visibility="{Binding PlayerSearchVisible, Converter={StaticResource BooleanToVisibilityConverter}}">
                <common:PlayerSearch DataContext="{Binding PlayerSearch}" />
            </Grid>
        </Grid>
    </telerik:RadBusyIndicator>

</UserControl>
