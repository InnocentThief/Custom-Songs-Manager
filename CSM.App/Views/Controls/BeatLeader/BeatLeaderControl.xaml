<UserControl x:Class="CSM.App.Views.Controls.BeatLeader.BeatLeaderControl"
             xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
             xmlns:d="http://schemas.microsoft.com/expression/blend/2008"
             xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
             xmlns:mc="http://schemas.openxmlformats.org/markup-compatibility/2006"
             xmlns:telerik="http://schemas.telerik.com/2008/xaml/presentation"
             xmlns:beatleader="clr-namespace:CSM.App.Views.Controls.BeatLeader"
             mc:Ignorable="d" 
             d:DesignHeight="450" d:DesignWidth="800">

    <telerik:RadBusyIndicator IsBusy="{Binding LoadingInProgress}" BusyContent="{Binding LoadingInProgressMessage}">
        <Grid>
            <Grid.RowDefinitions>
                <RowDefinition Height="40" />
                <RowDefinition Height="Auto" />
                <RowDefinition Height="*" />
            </Grid.RowDefinitions>
            
            <!-- Header -->
            <Grid Background="#88018574">
                <Grid.ColumnDefinitions>
                    <ColumnDefinition Width="*" />
                    <ColumnDefinition Width="Auto" />
                </Grid.ColumnDefinitions>
                <!-- Title -->
                <TextBlock Text="BeatLeader" Style="{StaticResource HeaderTitleStyle}" />
                <!-- Commands -->
                <telerik:RadToolBar Grid.Column="1" Style="{StaticResource HeaderToolBarStyle}" >
                    <!--<TextBlock Text="{Binding CustomLevelPath}" TextTrimming="CharacterEllipsis" Width="300" ToolTip="{Binding CustomLevelPath}"/>
                    <telerik:RadButton Command="{Binding OpenInFileExplorerCommand}" ToolTip="Open in File Explorer">
                        <telerik:RadGlyph Glyph="&#xe134;" />
                    </telerik:RadButton>
                    <telerik:RadButton Command="{Binding RefreshCommand}" ToolTip="Refresh">
                        <telerik:RadGlyph Glyph="&#xe103;" />
                    </telerik:RadButton>
                    <telerik:RadToolBarSeparator />-->
                    <telerik:RadButton ToolTip="Switch player">
                        <telerik:RadGlyph Glyph="&#xe801;" />
                    </telerik:RadButton>
                </telerik:RadToolBar>
            </Grid>
            
            <!-- Player -->
            <beatleader:PlayerControl Grid.Row="1" DataContext="{Binding Player}" />

            <!-- Scores -->
            
        </Grid>
    </telerik:RadBusyIndicator>

</UserControl>
