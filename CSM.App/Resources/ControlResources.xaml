<ResourceDictionary xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
                    xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
                    xmlns:telerik="http://schemas.telerik.com/2008/xaml/presentation"
                    >

    <Style x:Key="HeaderTitleStyle" TargetType="TextBlock">
        <Setter Property="FontSize" Value="14" />
        <Setter Property="FontWeight" Value="DemiBold" />
        <Setter Property="Foreground" Value="White" />
        <Setter Property="Margin" Value="10,0" />
        <Setter Property="VerticalAlignment" Value="Center" />
    </Style>

    <Style x:Key="HeaderToolBarStyle" TargetType="telerik:RadToolBar">
        <Setter Property="Background" Value="Transparent" />
        <Setter Property="BorderThickness" Value="0" />
        <Setter Property="GripVisibility" Value="Collapsed" />
        <Setter Property="OverflowButtonVisibility" Value="Collapsed" />
    </Style>
    
    <Style x:Key="RadGridViewStyle" TargetType="telerik:RadGridView">
        <Setter Property="AlternationCount" Value="2" />
        <Setter Property="AutoGenerateColumns" Value="False" />
        <Setter Property="BorderThickness" Value="0" />
        <Setter Property="FilteringDropDownStaysOpen" Value="True" />
        <Setter Property="FilteringMode" Value="Popup" />
        <Setter Property="FrozenColumnsSplitterVisibility" Value="Collapsed" />
        <Setter Property="RowIndicatorVisibility" Value="Collapsed" />
        <Setter Property="SelectionMode" Value="Single" />

    </Style>

</ResourceDictionary>